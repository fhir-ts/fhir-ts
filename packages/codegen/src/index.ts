/**
 * Says hello!
 * @param name - The name of the person (or thing) to say hello to
 * @returns a string representing your greeting
 *
 * @remarks
 * This function is only here temporarily, and will be removed as soon as this project has actual code.
 */
export function sayHello(name: string = 'World') {
  return `Hello, ${name}!`;
}
